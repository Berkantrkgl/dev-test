# https://kubernetes.io/docs/concepts/services-networking/service/
apiVersion: v1
kind: Service
metadata:
  name: argocd-demo
spec:
  selector:
    app: argocd-demo
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
